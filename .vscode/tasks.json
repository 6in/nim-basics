{
    "version": "0.1.0",
    "command": "nim",
    "args": [
        "c",
        "--out:debug/${fileBasename}.exe",
        "-r",
        "--threads:on",
        "${file}",
        "${workspaceRoot}/hello.txt",
        "--foo=a",
        "--bar=b",
        "-hoge:6in"
    ],
    "options": {
        "cwd": "${workspaceRoot}"
    },
    "isShellCommand": true
}