{
    "version": "0.1.0",
    "command": "nim",
    "args": [
        "c",
        "--out:debug/${fileBasename}.exe",
        "-r",
        "--threads:on",
        "${file}",
        "-h=localhost",
        "-u=postgres",
        "-p=postgres",
        "-port=5432",
        "-i=test.sql",
        "-db=sample",
        "-pname=abc"
    ],
    "options": {
        "cwd": "${workspaceRoot}"
    },
    "isShellCommand": true
}